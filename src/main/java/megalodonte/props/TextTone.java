package megalodonte.props;

//Vai fazer uso da cor de tema escolhida, exemplo PRIMARY vai ser convertido para Theme.colors().primary()
public enum TextTone {
    PRIMARY,
    SECONDARY,
    DISABLED,
    INVERTED
}
